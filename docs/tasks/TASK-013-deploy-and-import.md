---
id: TASK-013
project: agora
title: Деплой каталога на общий VPS + импорт 739 компаний
owner: claude-lead
assignee: grok
status: inbox
created: 2026-09-06
repo: agora-catalog
branch: task/013-deploy
---

## Состояние на входе

`main` собран и рабочий: `tsc` чист в трёх пакетах, 78 тестов из 78 зелёные.
Шесть веток первой волны слиты. Осталось только выкатить и залить данные.

Уже сделано тимлидом, повторять не надо:
- база `agora_catalog` на сервере создана, роль `agora_catalog` заведена,
  креды в `/opt/agora-catalog/.env.dbcreds` (chmod 600);
- все миграции применены на живом PG14, 22 категории верхнего уровня и 50 подкатегорий засеяны;
- `deploy/` содержит nginx-конфиг, systemd-юнит и скрипт бэкапов;
- `docs/DEPLOY.md` — пошаговая инструкция, читать целиком.

## Goal

1. Поднять `apps/api` на общем VPS `178.88.115.213` под systemd за nginx с TLS.
2. Залить в базу 739 компаний из JSONL парсера.
3. Показать фактом, что публичный API и панель оператора работают на сервере.

## Данные для импорта

`D:\rauanprojects\agora-yamaps-parser\data\raw\yamaps_moscow_packaging_20260906.jsonl`
739 строк, 739 уникальных `oid`, 1.1 МБ. Телефон или сайт есть у 96.3%.

**`description_raw` пуст у всех 739** — с Яндекс.Карт описания не собрались.
Это не поломка: у 96.5% заполнен `features` (структурированные признаки товаров —
«Бумажные пакеты», «Пластиковая тара», «Скотч»), из них автомэппинг и берёт категории.
Генерация текстов описаний — отдельная задача, в эту не входит.

## Ограничения, нарушение = переделка

1. **Сервер общий.** Рядом 13 процессов PM2 чужих проектов (`trek`, `gemini-proxy`,
   `ticket-bot`, `avito-parser`, `krisha-*` и т.д.), чужие базы в том же Postgres
   (`ai_manager`, `berufsberatung`, `ticket_bot`). **Ничего чужого не трогать,
   не перезапускать, не гасить.** Наш процесс — systemd, не PM2.
2. **PostgreSQL 14.24, апгрейдить нельзя.** Синтаксис PG15+ не использовать.
3. Порт **8791**. 8787 занят мозгом AmanAI, 8788 — мозгом Агоры.
4. `MemoryMax=768M` в юните не повышать: 3 сентября сервер уже разгружали.
5. Импорт идёт через `npm run -w @agora/api import`, а не ручными INSERT.

## Приёмка — только выводом команд

- `systemctl is-active agora-catalog-api` → `active`, и он же поднимается после
  `systemctl restart`.
- `curl -s https://agora-catalog.178.88.115.213.sslip.io/v1/companies | head -c 300`
  → живой JSON с компаниями, `total` близко к 739.
- `curl -s ".../v1/search?q=гофрокороба"` → непустой результат.
- `curl -s ".../v1/categories" ` → 22 категории верхнего уровня.
- Панель `/admin/login` отдаёт форму, вход работает (создай оператора через
  `npm run -w @agora/api seed:admin`).
- В отчёте: сколько компаний создано, сколько обновлено, сколько осталось
  без категории, сколько без телефона.
- `free -m` до и после запуска — показать, что съели не больше ~700 МБ.

## Границы

Фронт (`apps/web`) на Vercel не выкатывать — это отдельно и решает оунер.
Схему не менять. Описания компаний не генерировать. DaData не трогать (снята).
