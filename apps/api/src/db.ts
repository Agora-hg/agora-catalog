/**
 * Модуль оставлен пустым намеренно.
 *
 * TASK-008 держал здесь своё подключение (node-postgres + пул) — тогда в main
 * ещё не было общего входа. Теперь подключение одно: `getDb()` из `@agora/db`,
 * оно же выбирает движок (PGlite в dev, node-postgres на сервере).
 * Тип `Db` тоже берётся оттуда, иначе он прибивался к node-postgres
 * и каталог перестал бы компилироваться на dev-базе.
 */
export type { Db } from '@agora/db'
